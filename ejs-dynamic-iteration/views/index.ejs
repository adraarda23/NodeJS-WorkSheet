<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS ile Dinamik Veri</title>
</head>
<body>
    <h1>For Döngüsü ile Dinamik Veri:</h1>
    <ul>
        <% for (let i = 0; i < data.length; i++) { %>
            <li><%= data[i] %></li>
        <% } %>
    </ul>

    <h1>forEach Döngüsü ile Dinamik Veri:</h1>
    <ul>
        <% data.forEach(item => { %>
            <li><%= item %></li>
        <% }); %>
    </ul>
</body>
</html>
